{
  "title": "Access Restriction Advanced",
  "quizzes": {
    "3008": {
      "question": "When you call a function like 'setEnabled(0xBob)' on the Transaction AllowList precompile from Solidity, what actually executes this function?",
      "options": [
        "The transaction sender's wallet software",
        "A smart contract deployed at the precompile address",
        "Go code built into the VM that is invoked when the EVM routes calls to the precompile's reserved address",
        "A special JavaScript runtime in the EVM"
      ],
      "correctAnswers": [
        2
      ],
      "hint": "Precompiles are not regular deployed contracts - they're VM-level implementations.",
      "explanation": "When you call a precompile function from Solidity, the EVM recognizes the call is targeting a reserved address (like 0x0200...0002) and routes it to Go code built into the VM implementation, not to a deployed smart contract. The PrecompileOverride mechanism checks if the address has a registered precompile module, and if so, invokes the module's Run() function with the call data. The function selector (first 4 bytes) is parsed to determine which Go function to execute, which then performs the requested operation (like setting a role) by reading and writing to the state database.",
      "chapter": "Access Restriction",
      "alternates": [
        {
          "question": "When the EVM routes a call to a precompile's reserved address, how does the Go code determine which specific function (like setEnabled vs readAllowList) to execute?",
          "options": [
            "The function name is passed as a string parameter in the transaction data",
            "Each function has a unique precompile address",
            "The EVM's Solidity compiler routes the call automatically",
            "Go code built into the VM parses the function selector from the first 4 bytes of the call data, matching it to the corresponding Go function"
          ],
          "correctAnswers": [
            3
          ],
          "hint": "Think about how Solidity encodes function calls — the same ABI encoding applies to precompile calls.",
          "explanation": "When a call reaches the precompile's Go implementation, it parses the function selector from the first 4 bytes of the call data (the keccak256 hash of the function signature, same as standard Solidity ABI encoding). For example, setEnabled(address) has a specific 4-byte selector that the Go code matches to execute the role-setting logic. The remaining bytes contain the ABI-encoded parameters (like the target address). This allows a single precompile address to expose multiple functions, just like a regular Solidity contract."
        },
        {
          "question": "Why are precompile function calls more gas-efficient than equivalent operations in a deployed Solidity smart contract?",
          "options": [
            "Precompiles skip EVM bytecode interpretation entirely — the call goes directly to optimized native Go code, avoiding the overhead of the EVM instruction-by-instruction execution engine",
            "Precompiles use less storage space on the blockchain",
            "Go code at the precompile's reserved address is invoked when the EVM routes calls to the precompile, making it faster",
            "Precompiles are exempt from gas metering"
          ],
          "correctAnswers": [
            0
          ],
          "hint": "Compare the execution path: EVM bytecode interpretation vs. direct native code execution.",
          "explanation": "Precompiles are more gas-efficient because they bypass the EVM's bytecode interpreter entirely. When the EVM processes a regular smart contract call, it loads the contract's bytecode and executes it opcode by opcode, with each opcode consuming gas according to the gas schedule. Precompile calls skip this entirely — the EVM recognizes the reserved address and directly invokes the native Go implementation, which runs as compiled machine code. This eliminates interpretation overhead and allows operations like cryptographic computations or state management to execute much faster, reflected in lower fixed gas costs (e.g., 5,000 for reads, 20,000 for writes)."
        }
      ]
    },
    "3009": {
      "question": "According to the lesson on direct precompile calls, what is the gas cost for writing a role (like setEnabled) to an allowlist precompile?",
      "options": [
        "10,000 gas",
        "5,000 gas",
        "50,000 gas",
        "20,000 gas"
      ],
      "correctAnswers": [
        3
      ],
      "hint": "Read operations are cheaper than write operations for precompiles.",
      "explanation": "According to the content, precompiles have fixed gas costs: Read role operations cost 5,000 gas, while Write role operations (like setEnabled, setAdmin, setManager) cost 20,000 gas. This 4x difference reflects the higher cost of state modifications compared to reads, consistent with general EVM gas pricing where writes are more expensive than reads.",
      "chapter": "Access Restriction",
      "alternates": [
        {
          "question": "What is the gas cost for a read operation (like readAllowList) on an allowlist precompile, and how does it compare to the write cost?",
          "options": [
            "Read costs 20,000 gas, same as write operations",
            "Writing a role like setEnabled costs 20,000 gas for any allowlist precompile",
            "Read costs 5,000 gas — exactly 4x cheaper than the 20,000 gas write cost, reflecting the lower computational cost of reading state vs. modifying it",
            "Read operations are free (0 gas)"
          ],
          "correctAnswers": [
            2
          ],
          "hint": "The ratio between read and write costs follows a consistent pattern in EVM gas pricing.",
          "explanation": "Read operations on allowlist precompiles cost 5,000 gas, while write operations cost 20,000 gas — a 4x difference. This 4:1 ratio is consistent with general EVM gas pricing philosophy where state modifications (SSTORE) are significantly more expensive than state reads (SLOAD). Reading a role only requires looking up a storage slot, while writing requires modifying the state trie, creating a new storage entry, and generating a state diff that must be propagated across validators and stored permanently."
        },
        {
          "question": "How does the 20,000 gas cost for a precompile write operation compare to the cost of a standard EVM SSTORE operation for a new storage slot?",
          "options": [
            "Read operations on precompiles cost 5,000 gas compared to the 20,000 gas write cost",
            "Precompile writes are always more expensive than SSTORE",
            "Precompile writes are identical in cost to SSTORE",
            "Precompile writes at 20,000 gas are the same as SSTORE for updating an existing non-zero slot, but cheaper than SSTORE for creating a new slot from zero (~22,100 gas), because the precompile uses fixed gas pricing"
          ],
          "correctAnswers": [
            3
          ],
          "hint": "Compare fixed precompile gas costs to the EVM's dynamic SSTORE gas schedule.",
          "explanation": "The precompile's fixed 20,000 gas for writes is comparable to SSTORE costs for updating existing storage (20,000 gas for changing a non-zero value) but cheaper than SSTORE for creating new storage from zero (which costs ~22,100 gas due to the cold storage access surcharge). Precompiles use fixed gas pricing rather than the dynamic SSTORE gas schedule, providing predictable costs. This fixed pricing is part of the precompile's design — gas costs are hardcoded in the Go implementation rather than calculated dynamically by the EVM's gas metering system."
        }
      ]
    },
    "3010": {
      "question": "Why does the Transaction AllowList check permissions at TWO different points: during mempool validation AND during execution? (Select all that apply)",
      "options": [
        "Both checks are identical and redundant",
        "The first check is a bug and will be removed in future versions",
        "The mempool check is for admins only while the execution check is for all users",
        "The mempool check provides fast, local rejection to prevent spam, while the execution check is canonical and ensures network-wide consensus"
      ],
      "correctAnswers": [
        3
      ],
      "hint": "Think about the difference between local validation and consensus-critical validation.",
      "explanation": "The Transaction AllowList enforces permissions at two checkpoints for different purposes. The mempool validation checkpoint provides immediate, fast rejection at the local node level - preventing unauthorized transactions from even entering the mempool and consuming network resources (spam prevention). The execution checkpoint during state transition is canonical and consensus-critical - all validators must agree on whether a transaction is authorized when it's actually executed in a block. This two-layer approach is necessary because state can change between checks - an admin could revoke your permission after your transaction enters the mempool but before it executes, so both checks are required for security and efficiency.",
      "chapter": "Access Restriction",
      "alternates": [
        {
          "question": "What could happen if the Transaction AllowList only had the execution-time check (during state transition) but NOT the mempool validation check? (Select all that apply)",
          "options": [
            "Nothing different — the execution check is sufficient for all purposes",
            "Unauthorized transactions would still enter the mempool and propagate across the network, consuming bandwidth and node resources before being rejected during block execution",
            "This creates a denial-of-service vector where an attacker could spam unauthorized transactions that waste network resources even though they will never succeed",
            "The blockchain would stop producing blocks"
          ],
          "correctAnswers": [
            1,
            2
          ],
          "hint": "Think about what happens between a transaction entering the mempool and being executed in a block.",
          "explanation": "Without the mempool validation check, unauthorized transactions would flood the network. They would enter the local mempool, propagate to other nodes' mempools, consume bandwidth and memory across the network, and only be rejected when a validator tries to execute them in a block. This creates a denial-of-service vector — an attacker could spam unauthorized transactions that waste network resources even though they'll never succeed. The mempool check prevents this by rejecting unauthorized transactions immediately at the local node, before they can propagate."
        },
        {
          "question": "Why can a transaction that passes the mempool check still fail at the execution-time check on the Transaction AllowList?",
          "options": [
            "Because an Admin could revoke the sender's role between when the transaction enters the mempool and when it executes in a block, changing the sender's permission state",
            "Because the mempool check and execution check use different validation logic",
            "Because mempool validation provides fast local rejection for spam prevention while execution provides canonical consensus-critical validation",
            "This cannot happen — if a transaction passes mempool validation, it always passes execution"
          ],
          "correctAnswers": [
            0
          ],
          "hint": "Blockchain state can change between the time a transaction enters the mempool and when it's actually executed.",
          "explanation": "A transaction can pass mempool validation but fail at execution because blockchain state is not static. Between the time a transaction enters the mempool and when a validator includes it in a block, other transactions can execute that change the allowlist state. For example, an Admin could call setNone(address) to revoke someone's Enabled role. The revocation transaction might be included in a block before the sender's pending transaction. When the sender's transaction finally executes, the execution-time check reads the updated state (role = None) and rejects it. This is why both checks are necessary — mempool for immediate filtering, execution for correctness."
        }
      ]
    },
    "3011": {
      "question": "How can a user be blocked from sending a transaction by the Transaction AllowList precompile even though they never directly interacted with the precompile?",
      "options": [
        "This is not possible - users must explicitly call the precompile to be blocked",
        "The wallet software blocks the transaction before it reaches the network",
        "Only smart contracts can be blocked, not regular transactions",
        "The EVM automatically reads the precompile's storage at hardcoded enforcement checkpoints during transaction processing, rejecting unauthorized senders"
      ],
      "correctAnswers": [
        3
      ],
      "hint": "Remember the difference between the precompile's storage role and its enforcement role.",
      "explanation": "Allowlist precompiles serve dual roles: they act as storage for address-to-role mappings AND provide automatic enforcement through hardcoded checkpoints in the EVM code. When you send ANY transaction (even a simple ETH transfer), the EVM automatically reads from the precompile's storage using GetState(precompileAddress, keccak256(senderAddress)) at enforcement points (mempool validation, state transition, and contract creation hook). If your address has no role or insufficient permissions, the transaction is rejected - without you ever calling the precompile directly. This automatic enforcement architecture ensures permissions are checked consistently throughout the transaction lifecycle.",
      "chapter": "Access Restriction",
      "alternates": [
        {
          "question": "How does the EVM determine a sender's allowlist role during automatic enforcement, without the sender calling the precompile?",
          "options": [
            "The EVM queries an external role management service",
            "The sender's role is embedded in the transaction signature",
            "It reads the precompile's state storage using GetState(precompileAddress, keccak256(senderAddress)), looking up the role value stored in the precompile's reserved storage slot for that address",
            "The EVM reads the precompile's storage at hardcoded enforcement checkpoints, rejecting unauthorized senders automatically"
          ],
          "correctAnswers": [
            2
          ],
          "hint": "Precompile roles are stored in EVM state — how is state accessed at a specific address?",
          "explanation": "During automatic enforcement, the EVM looks up the sender's role by calling GetState on the precompile's contract address with a storage key derived from keccak256(senderAddress). The precompile stores role assignments as a mapping from address hashes to role values (0-3) in regular EVM storage slots. This is the same state that gets modified when setEnabled or setAdmin is called. The enforcement hooks simply read this storage at hardcoded checkpoints — no explicit call to the precompile contract is needed because the EVM can directly read any contract's storage."
        },
        {
          "question": "If the Contract Deployer AllowList is enabled but the Transaction AllowList is NOT, what happens when an address not on the deployer allowlist tries to send a simple AVAX transfer?",
          "options": [
            "The transfer fails because the address is not on any allowlist",
            "The EVM automatically reads the precompile's storage at hardcoded enforcement checkpoints and rejects unauthorized senders",
            "The transfer is queued until an Admin approves it",
            "The transfer succeeds — Transaction AllowList is not enabled so there's no restriction on submitting transactions; the Contract Deployer AllowList only restricts contract creation, not value transfers"
          ],
          "correctAnswers": [
            3
          ],
          "hint": "Each precompile has its own independent enforcement scope — what does the Contract Deployer AllowList restrict?",
          "explanation": "The transfer succeeds because each allowlist precompile has independent enforcement scope. The Contract Deployer AllowList only hooks into the CanCreateContract callback — it only blocks CREATE/CREATE2 opcodes (contract deployments). Simple AVAX transfers and contract calls don't trigger this hook. Since the Transaction AllowList is not enabled, there's no restriction on who can submit transactions. The address could send AVAX, call existing contracts, and interact with the chain freely — it just can't deploy new contracts."
        }
      ]
    },
    "3012": {
      "question": "What is the main risk of removing your own wallet from the Admin role on both allowlist precompiles, as done in this exercise?",
      "options": [
        "The blockchain will stop producing blocks",
        "Other users will gain access to your wallet",
        "You may lock yourself out - unable to grant permissions back or perform any transactions/deployments if you also remove yourself from Enabled role",
        "Your wallet balance will be reset to zero"
      ],
      "correctAnswers": [
        2
      ],
      "hint": "Consider what happens when you no longer have any role on the allowlists.",
      "explanation": "Removing your own wallet from the Admin role on allowlist precompiles creates a lock-out scenario. If you're the only Admin and you remove yourself (especially if you set your role to None), you lose the ability to grant permissions back to yourself or anyone else. Without Admin privileges, you can't call setAdmin, setManager, or setEnabled. If you also lack Enabled role, you can't send transactions (Transaction AllowList) or deploy contracts (Contract Deployer AllowList). This demonstrates why network upgrades via upgrade.json exist - as a recovery mechanism when permission misconfigurations lock out all administrators.",
      "chapter": "Access Restriction",
      "alternates": [
        {
          "question": "If a chain has both allowlist precompiles enabled and all Admin addresses are removed (set to None), what is the ONLY recovery mechanism?",
          "options": [
            "Wait for the blockchain to automatically reassign admin roles",
            "Contact Avalanche support to manually reset permissions",
            "A network upgrade via upgrade.json — disable the precompiles and re-enable them with new adminAddresses, since no on-chain method can restore admin access once all admins are removed",
            "Lock yourself out by removing your own wallet from Admin on both precompiles and lose the ability to grant permissions back"
          ],
          "correctAnswers": [
            2
          ],
          "hint": "If no Admin exists on-chain, what off-chain mechanism can reconfigure the precompile?",
          "explanation": "When all Admin addresses are removed from both allowlist precompiles, there is no on-chain way to recover — nobody can call setAdmin because only existing Admins can do that. The only recovery mechanism is a network upgrade via upgrade.json. A validator operator with filesystem access can: (1) disable both precompiles by adding entries with 'disable: true' at a future timestamp, (2) re-enable them with new adminAddresses in subsequent entries, (3) restart the node. This is why network upgrades are described as a 'recovery mechanism for permission misconfigurations' in the course."
        },
        {
          "question": "In the exercise where you intentionally lock yourself out, what specific sequence of operations causes the lockout?",
          "options": [
            "Calling setNone on your own address on the Transaction AllowList (removing transaction permission) and then calling setNone on your own address on the Contract Deployer AllowList (removing deploy permission)",
            "Shutting down all validator nodes on the network",
            "Deploying a smart contract that modifies the precompile storage directly",
            "You may lock yourself out and be unable to grant permissions back or perform any transactions if you remove yourself from Enabled role"
          ],
          "correctAnswers": [
            0
          ],
          "hint": "Think step by step about which permissions you lose with each setNone call.",
          "explanation": "The lockout occurs by calling setNone on your own address on both precompiles. First, you call setNone(yourAddress) on the Contract Deployer AllowList — you can still send transactions but can no longer deploy contracts. Then you call setNone(yourAddress) on the Transaction AllowList — now you can't even send transactions, which means you also can't call any precompile functions to restore your roles. The order matters: you must remove deploy permission first (while you can still send transactions), then remove transaction permission. After both calls, your address has role 0 (None) on both precompiles, completely locked out."
        }
      ]
    },
    "3013": {
      "question": "After intentionally removing your Admin and Enabled roles from both allowlist precompiles, what should you observe when attempting to send a transaction and deploy a contract?",
      "options": [
        "Both operations are queued for manual approval",
        "Both operations fail with authorization errors because the allowlist precompiles block your address",
        "Both operations succeed but with higher gas costs",
        "The transaction succeeds but the deployment fails"
      ],
      "correctAnswers": [
        1
      ],
      "hint": "Remember that enforcement happens automatically at hardcoded checkpoints.",
      "explanation": "After removing your roles from both allowlist precompiles, you should observe both operations failing with authorization errors. When you attempt a normal transaction (value transfer), the Transaction AllowList will block it at the mempool validation and/or execution checkpoint, returning an error like 'sender not allow listed'. When you attempt a contract deployment, the Contract Deployer AllowList will block it via the CanCreateContract hook, returning an error like 'tx.origin is not authorized to deploy a contract'. Both transactions will consume some gas but fail before executing their intended operations. This demonstrates the enforcement mechanisms in action.",
      "chapter": "Access Restriction",
      "alternates": [
        {
          "question": "When testing the lockout scenario, what specific error message or behavior should you expect when attempting to send a transaction from a removed address?",
          "options": [
            "The transaction processes normally but with a warning",
            "The transaction processes but gets reverted after 24 hours",
            "An error indicating 'sender not allow listed' or similar authorization failure, returned by the node before the transaction enters the mempool",
            "Both operations fail with authorization errors because allowlist precompiles block your address"
          ],
          "correctAnswers": [
            2
          ],
          "hint": "The mempool validation check provides the first rejection point with a specific error.",
          "explanation": "When you attempt to send a transaction from an address that has been removed from the Transaction AllowList (role = None), the node rejects it at the mempool validation checkpoint with an error message like 'sender not allow listed' or 'tx.origin is not authorized.' This rejection happens immediately at the local node — the transaction never enters the mempool or propagates to other nodes. Similarly, attempting a contract deployment when removed from the Contract Deployer AllowList returns 'tx.origin is not authorized to deploy a contract.' These specific error messages help developers diagnose permission issues."
        },
        {
          "question": "During the lockout exercise, why is it important to test BOTH a value transfer AND a contract deployment after removing your roles?",
          "options": [
            "To verify that both allowlist precompiles enforce independently — the Transaction AllowList blocks transfers and the Contract Deployer AllowList blocks deployments, confirming both are active and enforcing correctly",
            "Testing only one operation is sufficient to verify the lockout",
            "Both operations fail with authorization errors because the allowlist precompiles block your address from all activity",
            "Value transfers and deployments use the same enforcement mechanism"
          ],
          "correctAnswers": [
            0
          ],
          "hint": "Each precompile protects a different type of operation — testing both confirms dual enforcement.",
          "explanation": "Testing both operations independently verifies that each allowlist precompile enforces its own scope correctly. The Transaction AllowList blocks the value transfer via its mempool/execution checkpoints, while the Contract Deployer AllowList blocks the deployment via the CanCreateContract hook. Testing only one would leave the other unverified. This exercise demonstrates the dual-layer access control — an address might be enabled on one precompile but not the other, and both must be working correctly for the L1's security model to hold."
        }
      ]
    },
    "3014": {
      "question": "What is the critical rule about timestamps when creating entries in upgrade.json for network upgrades? (Select all that apply)",
      "options": [
        "Timestamps must be in the future relative to the current chain head, or the node may fail to start",
        "Timestamps must always be set to zero",
        "Timestamps are optional and can be omitted",
        "Timestamps must be in the past relative to genesis"
      ],
      "correctAnswers": [
        0
      ],
      "hint": "Think about what happens when a node encounters an upgrade entry with a past timestamp.",
      "explanation": "When creating network upgrade entries in upgrade.json, timestamps must be in the future relative to the current chain head. If a node encounters an upgrade entry with a blockTimestamp that is in the past relative to the head of the chain, it can fail to start. This is a critical rule because the node expects upgrades to activate at future blocks. The practical takeaway is to set timestamps to (current time + a few minutes) rather than 'right now' to ensure the upgrade activates properly. Additionally, upgrade entries must be in increasing timestamp order.",
      "chapter": "Access Restriction",
      "alternates": [
        {
          "question": "What happens if you place an upgrade.json entry with a blockTimestamp that has already passed relative to the current chain head?",
          "options": [
            "The upgrade activates immediately at the next block",
            "The upgrade is silently ignored",
            "The node may fail to start or produce errors during startup because it cannot retroactively apply upgrades to already-processed blocks",
            "Timestamps must be in the future relative to the current chain head, or the node may fail to start"
          ],
          "correctAnswers": [
            2
          ],
          "hint": "Consider what it means for a node to encounter an upgrade that should have happened before the current block.",
          "explanation": "If a node encounters an upgrade.json entry with a blockTimestamp that has already passed (relative to the chain head), it may fail to start. The node cannot retroactively apply precompile changes to blocks that were already processed under different rules — this would break consensus with other nodes that processed those blocks without the upgrade. The node detects this inconsistency during startup validation and refuses to proceed. This is why the course emphasizes setting timestamps to 'current time + a few minutes' to ensure the upgrade activates at a future block."
        },
        {
          "question": "If you need to add multiple precompile changes in upgrade.json, what ordering requirement must the entries follow?",
          "options": [
            "Entries can be in any order — the node sorts them automatically",
            "Entries must be in decreasing timestamp order (newest first)",
            "Entries in the precompileUpgrades array must be in increasing timestamp order, with each entry having a later blockTimestamp than the previous one",
            "Timestamps must be in the future relative to the current chain head to avoid node startup failure"
          ],
          "correctAnswers": [
            2
          ],
          "hint": "Think about how a node processes sequential upgrades in chronological order.",
          "explanation": "Entries in the precompileUpgrades array must be in increasing (ascending) timestamp order. Each entry's blockTimestamp must be later than the previous entry's. This is because the node processes upgrades sequentially as the chain progresses — it needs to know unambiguously which upgrade applies at each point in time. If entries were out of order, the node couldn't determine the correct state at any given block. This chronological ordering also supports the append-only requirement: new upgrades are always added at the end with later timestamps."
        }
      ]
    },
    "3015": {
      "question": "Why is setting up a Docker validator necessary for performing network upgrades, rather than using BuilderHub hosted nodes?",
      "options": [
        "Docker validators cost less than hosted nodes",
        "BuilderHub doesn't support Avalanche L1s",
        "Docker validators are more secure than hosted nodes",
        "Docker validators allow direct filesystem access to edit upgrade.json and restart the node, which BuilderHub hosted nodes don't provide"
      ],
      "correctAnswers": [
        3
      ],
      "hint": "Think about what file access is required to create and apply network upgrades.",
      "explanation": "Setting up a Docker validator is necessary for network upgrades because you need direct access to the node's configuration files - specifically, you must be able to create or edit upgrade.json in the chain's config directory (~/.avalanchego/configs/chains/<BLOCKCHAIN_ID>/) and then restart the node to apply the changes. BuilderHub hosted nodes are managed services that abstract away the filesystem for convenience, but this means you can't directly access or modify configuration files. Running your own Docker validator gives you full filesystem access and control over the node lifecycle (start/stop/restart), which is essential for implementing network upgrades.",
      "chapter": "Access Restriction",
      "alternates": [
        {
          "question": "What specific Docker command or setup is typically used to run an Avalanche validator node with filesystem access for upgrade.json?",
          "options": [
            "Docker Compose with volume mounts that map the host's ~/.avalanchego directory into the container, giving the validator access to configuration files while maintaining container isolation",
            "A standard Docker run command with no volume mounts",
            "Docker validators provide direct filesystem access to edit upgrade.json, which BuilderHub hosted nodes don't provide",
            "Docker is not compatible with AvalancheGo — you must use bare metal"
          ],
          "correctAnswers": [
            0
          ],
          "hint": "Docker uses a specific mechanism to share files between the host and container.",
          "explanation": "Running an Avalanche validator in Docker typically uses Docker Compose with volume mounts (e.g., -v ~/.avalanchego:/root/.avalanchego). Volume mounts map a directory on the host machine into the container, allowing you to edit files like upgrade.json on the host and have them immediately accessible inside the running container. This gives you the best of both worlds: Docker's container isolation and reproducibility, plus direct filesystem access for configuration changes. After editing upgrade.json on the host, you restart the container to apply the changes."
        },
        {
          "question": "Besides upgrade.json modifications, what other operational tasks require filesystem access that BuilderHub hosted nodes cannot provide? (Select all that apply)",
          "options": [
            "Viewing real-time node logs stored in the local log directory for debugging",
            "Sending transactions to the network",
            "BuilderHub hosted nodes don't allow direct access to configuration files needed for network upgrades",
            "Configuring custom chain-specific settings in the chain's config.json file"
          ],
          "correctAnswers": [
            0,
            3
          ],
          "hint": "Think about what other configuration and diagnostic files live in the node's data directory.",
          "explanation": "Beyond upgrade.json, there are other operational tasks requiring filesystem access: (1) Viewing node logs in the log directory for real-time debugging and monitoring, (2) Configuring chain-specific settings in config.json (like adjusting gas limits, enabling pruning, or setting log levels per chain), (3) Managing staking keys and certificates, and (4) Accessing the database for backup or recovery. BuilderHub abstracts these away for convenience, but advanced operators need direct filesystem access for these tasks, making self-hosted Docker validators necessary for production L1 operations."
        }
      ]
    },
    "3016": {
      "question": "Why must the precompileUpgrades array in upgrade.json be treated as append-only after upgrades activate?",
      "options": [
        "Append-only is just a recommendation, not a requirement",
        "To make the file easier to read",
        "Because editing or removing activated upgrade entries can cause the node to fail on startup",
        "To save disk space on the validator"
      ],
      "correctAnswers": [
        2
      ],
      "hint": "Consider what happens when a node tries to validate historical blocks against modified upgrade configurations.",
      "explanation": "The precompileUpgrades array must be treated as append-only after upgrades activate because the node uses this configuration to validate the chain's history. Once an upgrade activates (after its blockTimestamp has passed and blocks are accepted), that entry becomes part of the chain's consensus rules. If you edit or remove an old entry, the node won't be able to properly validate historical blocks - it will detect an inconsistency between what it expects (based on the original upgrade) and what it finds (based on the modified upgrade.json), potentially causing it to refuse to start. The safe pattern is to always append new upgrade entries while preserving all previous ones exactly as they were.",
      "chapter": "Access Restriction",
      "alternates": [
        {
          "question": "If you accidentally delete an activated upgrade entry from upgrade.json, what happens when the node tries to sync past the block where that upgrade was originally applied?",
          "options": [
            "The node skips the deleted upgrade and continues syncing normally",
            "The node fails because it processes historical blocks under different consensus rules than the original validators used, creating an irreconcilable state mismatch",
            "Editing or removing activated upgrade entries can cause the node to fail on startup",
            "The node automatically reconstructs the deleted entry from peer data"
          ],
          "correctAnswers": [
            1
          ],
          "hint": "Nodes must replay history with the same rules that were in effect when blocks were originally produced.",
          "explanation": "When a node syncs (or restarts and validates) the chain, it replays blocks sequentially, applying the same consensus rules that were in effect at each block height. If an activated upgrade entry is deleted, the node processes blocks after the original activation timestamp WITHOUT the upgrade rules — but those blocks were produced by validators WITH the upgrade active. This creates a state mismatch: the replaying node computes different state roots than what the blocks contain, causing validation failure. The node cannot reconcile this difference and refuses to continue."
        },
        {
          "question": "What is the correct way to undo the effect of a previously activated precompile upgrade?",
          "options": [
            "Delete the original entry from upgrade.json and restart the node",
            "Append a NEW entry with a future timestamp that disables or reconfigures the precompile, keeping all previous entries intact",
            "The precompileUpgrades array in upgrade.json must be treated as append-only because removing activated entries causes node failures",
            "Modify the original entry to set 'disable: true' in-place"
          ],
          "correctAnswers": [
            1
          ],
          "hint": "The append-only rule means you can only add new entries — how do you reverse a previous change?",
          "explanation": "To undo a precompile upgrade, you append a NEW entry at the end of the precompileUpgrades array with a future timestamp and the desired new configuration (e.g., 'disable: true' to deactivate, or new admin addresses to reconfigure). All previous entries must remain exactly as they were. For example, if entry 1 enabled the Transaction AllowList at timestamp T1, you add entry 2 disabling it at timestamp T2 > T1. The chain history remains consistent: the precompile was active from T1 to T2, and disabled after T2."
        }
      ]
    },
    "3017": {
      "question": "After successfully disabling the Transaction AllowList and Contract Deployer AllowList via upgrade.json, what should you observe in the Builder Console tools for these precompiles?",
      "options": [
        "The tools display 'not available' or 'not activated' state instead of the full allowlist UI",
        "The tools show error messages about missing permissions",
        "The tools show all addresses as having Admin role",
        "The tools function normally but with a warning banner"
      ],
      "correctAnswers": [
        0
      ],
      "hint": "When a precompile is disabled, it's no longer active on the network.",
      "explanation": "After disabling the precompiles via upgrade.json with 'disable: true' and restarting the node, the Builder Console tools should detect that the precompiles are no longer active on the network. Instead of rendering the full allowlist UI (with functions like readAllowList, setEnabled, etc.), the tools should display a 'not available' or 'precompile not activated' state - typically showing an informational message and image. This is different from the case where the precompile is active but your address lacks permissions. The key verification is that the tools detect the precompile's absence at the protocol level, confirming that the network upgrade successfully deactivated the functionality.",
      "chapter": "Access Restriction",
      "alternates": [
        {
          "question": "After disabling allowlist precompiles via upgrade.json with 'disable: true', can existing smart contracts that previously called these precompiles still execute?",
          "options": [
            "Calls to the disabled precompile addresses will fail or return empty/zero data since the precompile is no longer active — contracts relying on allowlist checks would need to handle this gracefully",
            "Existing contracts continue working exactly as before — disabled precompiles have no effect",
            "The Builder Console tools display 'not available' or 'not activated' state instead of the full allowlist UI",
            "Existing contracts are automatically deleted from the chain"
          ],
          "correctAnswers": [
            0
          ],
          "hint": "Think about what happens when a contract tries to call an address that no longer has a precompile implementation.",
          "explanation": "When a precompile is disabled, calls to its reserved address no longer route to the Go implementation. Any existing smart contract that calls the disabled precompile address will receive unexpected results — typically empty return data or a revert, since there is no longer code handling the call at that address. This is an important consideration when disabling precompiles: any contracts that depend on allowlist functionality (e.g., checking roles before performing actions) may break. Developers should design contracts to handle the case where a precompile might be disabled in the future."
        },
        {
          "question": "What does the 'disable: true' field in an upgrade.json entry specifically cause at the protocol level when the specified timestamp is reached? (Select all that apply)",
          "options": [
            "It removes the precompile's reserved address from the blockchain entirely",
            "The precompile module is unregistered from the VM's PrecompileOverride map, so calls to its address are no longer routed to Go code",
            "The Builder Console shows 'not available' or 'not activated' when the precompile is disabled",
            "The precompile's storage is destructed (cleared), wiping all stored data including role assignments"
          ],
          "correctAnswers": [
            1,
            3
          ],
          "hint": "The disable process does two things: unregisters the module and clears its state.",
          "explanation": "When 'disable: true' activates at the specified timestamp, two things happen: (1) The precompile module is unregistered from the VM's PrecompileOverride map, meaning calls to its reserved address are no longer routed to Go code — they become regular (empty) address calls. (2) The precompile's storage is destructed (cleared), wiping all stored data including role assignments. This storage clearing is why re-enabling requires fresh adminAddresses — no previous role data survives the disable. The address itself still exists on the blockchain, but there's nothing behind it."
        }
      ]
    },
    "3018": {
      "question": "When re-enabling allowlist precompiles via upgrade.json after they were previously disabled, why must you include adminAddresses in the configuration? (Select all that apply)",
      "options": [
        "To maintain backwards compatibility with the old configuration",
        "adminAddresses are optional and not required",
        "Because disabling cleared the precompile's storage, so re-enabling is a fresh initialization that requires setting initial admin addresses",
        "adminAddresses automatically copy from the genesis configuration"
      ],
      "correctAnswers": [
        2
      ],
      "hint": "Think about what happens to the precompile's state when it's disabled.",
      "explanation": "When you disable a precompile, it destructs (clears) its storage as part of the deactivation process. This means all role assignments are wiped out. When you re-enable the precompile via a later upgrade.json entry, it's effectively a fresh initialization from scratch - there are no existing Admin, Manager, or Enabled addresses. Therefore, you must include adminAddresses in the re-enable configuration to set initial administrators who can then manage roles. Without this, the precompile would be active but nobody would have permission to use it or manage access, creating an immediate lock-out scenario.",
      "chapter": "Access Restriction",
      "alternates": [
        {
          "question": "When re-enabling a precompile via upgrade.json, can you restore it with a different set of initial admins than the original genesis configuration?",
          "options": [
            "No — re-enabled precompiles must use the same admin addresses as the genesis config",
            "Yes — the re-enable entry in upgrade.json can specify any addresses as adminAddresses since disabling cleared all previous state and it starts fresh",
            "Disabling cleared the precompile's storage, so re-enabling requires setting initial admin addresses from scratch",
            "The genesis admin addresses are automatically restored"
          ],
          "correctAnswers": [
            1
          ],
          "hint": "Since the disable wiped all state, the re-enable is essentially a fresh deployment.",
          "explanation": "Yes, when re-enabling a precompile after it was disabled, you can specify any addresses in the adminAddresses field — they don't need to match the original genesis configuration. Since disabling destructs (clears) all precompile storage including role assignments, re-enabling is functionally equivalent to a fresh initialization. This is actually a powerful feature: if the original admins' keys were compromised or the admin set needs to change, re-enabling via upgrade.json lets you start with a completely new set of administrators. You can also add managerAddresses and enabledAddresses in the re-enable entry."
        },
        {
          "question": "What would happen if you re-enable an allowlist precompile via upgrade.json but forget to include any adminAddresses in the configuration?",
          "options": [
            "The previous admin addresses are automatically restored from genesis",
            "Disabling a precompile clears its storage, so re-enabling requires setting initial admin addresses to avoid a fresh lockout",
            "The node rejects the configuration and fails to start",
            "The precompile activates but with no admins — creating an immediate lockout where nobody can manage roles, effectively making the precompile permanently restrictive since no new permissions can ever be granted"
          ],
          "correctAnswers": [
            3
          ],
          "hint": "If no one has Admin role, can anyone ever call setEnabled or setAdmin?",
          "explanation": "If you re-enable a precompile without specifying adminAddresses, the precompile activates with empty role mappings — no address has any role. Since only Admins can call setAdmin, setManager, or setEnabled, and no Admin exists, no one can ever grant any permissions. If it's the Transaction AllowList, no one can send transactions. If it's the Contract Deployer AllowList, no one can deploy contracts. This creates an immediate, permanent lockout that can only be resolved by another network upgrade via upgrade.json to disable and re-enable with proper adminAddresses."
        }
      ]
    },
    "3019": {
      "question": "After successfully re-enabling the allowlist precompiles via upgrade.json with your wallet in adminAddresses, what should you be able to do?",
      "options": [
        "Only read roles but not modify them",
        "Only send transactions but not deploy contracts",
        "Read roles, modify roles via setAdmin/setManager/setEnabled, send transactions, and deploy contracts",
        "View the allowlist UI but not interact with the blockchain"
      ],
      "correctAnswers": [
        2
      ],
      "hint": "Consider what capabilities the Admin role grants you on both precompiles.",
      "explanation": "After successfully re-enabling both allowlist precompiles with your wallet address in adminAddresses, you should have full Admin role on both the Transaction AllowList and Contract Deployer AllowList. This means you can: (1) read roles using readAllowList, (2) modify roles using setAdmin, setManager, setEnabled, and setNone functions, (3) send transactions because you're authorized on the Transaction AllowList, and (4) deploy contracts because you're authorized on the Contract Deployer AllowList. The Builder Console tools should render normally (no 'not available' state), and both blockchain operations and allowlist management should succeed.",
      "chapter": "Access Restriction",
      "alternates": [
        {
          "question": "After re-enabling the allowlist precompiles, how can you verify that the re-enable was successful using the Builder Console?",
          "options": [
            "The Builder Console will show an upgrade history log",
            "The Builder Console tools should render the full allowlist UI (not the 'not available' state), and calling readAllowList with your address should return 3 (Admin), confirming both the precompile is active and your role is set",
            "Read roles, modify roles, send transactions, and deploy contracts are all available with full Admin access after re-enabling",
            "The Builder Console displays a green checkmark next to the precompile name"
          ],
          "correctAnswers": [
            1
          ],
          "hint": "What two things change when a precompile goes from disabled to re-enabled with your address as admin?",
          "explanation": "Successful re-enablement can be verified in two ways through the Builder Console: (1) The precompile tools should render the full interactive UI with all management functions (setAdmin, setManager, setEnabled, readAllowList) instead of the 'not available/not activated' state shown when disabled. (2) Calling readAllowList with your wallet address should return 3 (Admin), confirming your address was properly configured in the adminAddresses of the re-enable entry. Both checks together confirm the precompile is active at the protocol level AND your role was correctly initialized."
        },
        {
          "question": "After re-enabling both allowlist precompiles with your address as Admin, what is the first thing you should do to ensure the L1 is fully operational?",
          "options": [
            "Immediately disable the precompiles again to test the full cycle",
            "Grant Enabled role to any other addresses that need to transact or deploy on the L1, since the re-enable starts with a clean slate where only the admin addresses have permission",
            "Full Admin access means you can read roles, modify roles, send transactions, and deploy contracts",
            "Nothing — all previous permissions are automatically restored"
          ],
          "correctAnswers": [
            1
          ],
          "hint": "Remember that disabling wiped all previous role assignments — who has permission right now?",
          "explanation": "After re-enabling with a clean slate, only the addresses specified in adminAddresses have any role. All previously Enabled, Manager, and Admin addresses from before the disable have lost their permissions. The first operational step should be to re-grant roles to addresses that need them: use setEnabled for addresses that need basic transaction/deployment access, setManager for delegated administrators, and setAdmin for other trusted administrators. Without this step, the L1 is operational for admins but locked out for everyone else."
        }
      ]
    },
    "3020": {
      "question": "What is the key lesson from the complete cycle of enabling, disabling, and re-enabling allowlist precompiles via network upgrades?",
      "options": [
        "Network upgrades via upgrade.json provide a recovery mechanism for permission misconfigurations and allow dynamic adjustment of L1 behavior without redeploying the chain",
        "Precompiles should never be disabled once enabled",
        "Only Docker validators can use precompiles",
        "Genesis configuration cannot be changed after chain launch"
      ],
      "correctAnswers": [
        0
      ],
      "hint": "Think about why this upgrade capability is important for real-world L1 operations.",
      "explanation": "The complete cycle demonstrates that network upgrades via upgrade.json provide a powerful recovery and adjustment mechanism for Avalanche L1s. If you misconfigure permissions at genesis (like the exercise where you locked yourself out) or need to change L1 behavior post-launch, you can use upgrade.json to disable problematic precompiles, re-enable them with corrected configurations, or activate entirely new precompiles. This pattern applies to all Subnet-EVM precompiles (fee manager, native minter, reward manager, etc.), not just allowlists. The ability to perform coordinated network upgrades without redeploying the chain is essential for real-world L1 operations where requirements evolve and mistakes need correction.",
      "chapter": "Access Restriction",
      "alternates": [
        {
          "question": "The enable-disable-re-enable cycle demonstrated in the course applies to all Subnet-EVM precompiles, not just allowlists. Which other precompiles can be managed this way? (Select all that apply)",
          "options": [
            "Fee Manager, which allows dynamic adjustment of gas fee parameters post-launch",
            "Network upgrades via upgrade.json provide a recovery mechanism for misconfigurations and allow dynamic L1 behavior adjustment",
            "Native Minter, which controls the creation of new native tokens on the L1",
            "Only allowlist precompiles support the upgrade.json mechanism"
          ],
          "correctAnswers": [
            0,
            2
          ],
          "hint": "The upgrade.json mechanism works with any Subnet-EVM precompile, not just access control ones.",
          "explanation": "The upgrade.json mechanism applies to ALL Subnet-EVM precompiles, not just allowlists. Fee Manager allows dynamic adjustment of gas fees (base fee, min/max gas price) post-launch. Native Minter controls creation of new native tokens, enabling dynamic token supply management. Reward Manager configures how block rewards are distributed. Contract Deployer AllowList and Transaction AllowList control access. All of these can be enabled, disabled, and reconfigured via upgrade.json entries with future timestamps, following the same append-only, chronological ordering rules."
        },
        {
          "question": "Why is the ability to perform network upgrades without redeploying the chain particularly important for production Avalanche L1s?",
          "options": [
            "It's mainly useful for testing environments only",
            "It allows L1 operators to adapt to evolving requirements — fixing permission misconfigurations, adjusting fee parameters for changing market conditions, enabling new features, and responding to security incidents without chain downtime or data loss",
            "Network upgrades via upgrade.json provide a recovery mechanism that allows dynamic adjustment of L1 behavior without redeployment",
            "Redeploying chains is simple and costs nothing"
          ],
          "correctAnswers": [
            1
          ],
          "hint": "Consider the real-world operational challenges of running a blockchain in production.",
          "explanation": "Production L1s face evolving requirements that cannot all be anticipated at genesis. Regulatory requirements may change (requiring new access controls), gas fees may need adjustment for changing usage patterns, security incidents may require emergency precompile changes, and new features may need to be added as the ecosystem grows. Without network upgrades, the only option would be deploying an entirely new chain — requiring data migration, updating all integrations, and disrupting users. upgrade.json provides a mechanism to modify chain behavior in-place, preserving all existing state, contracts, and balances while adapting the chain's configuration."
        }
      ]
    }
  }
}